// Object.keys()
// provides all the keys within an object in array form

const kayObj = {
	firstName: 'kay',
	currentCountry: 'usa',
	hometown: 'minneapolis',
	numPets: 2,
	likesDogs: true,
	key: '🔑'
}

console.log(Object.keys(kayObj).length)
console.log(Object.values(kayObj))
